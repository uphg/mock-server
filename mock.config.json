{
  "port": 3001,
  "baseUrl": "/api",
  "delay": 0,
  "cors": true,
  "mockDir": "./data",
  "routeDefaults": [
    {
      "name": "api-headers",
      "description": "API通用响应头配置",
      "config": {
        "headers": {
          "Content-Type": "application/json",
          "X-API-Version": "v1",
          "X-Response-Time": "{{responseTime}}ms"
        }
      },
      "includes": ["/users*", "/products*"],
      "excludes": ["/error"]
    },
    {
      "name": "slow-response",
      "description": "慢响应配置",
      "config": {
        "delay": 1000,
        "headers": {
          "X-Slow-Response": "true"
        }
      },
      "includes": ["/products*"]
    },
    {
      "name": "cors-headers",
      "description": "CORS相关头部配置",
      "config": {
        "headers": {
          "Access-Control-Allow-Origin": "*",
          "Access-Control-Allow-Methods": "GET, POST, PUT, DELETE, OPTIONS",
          "Access-Control-Allow-Headers": "Content-Type, Authorization"
        }
      },
      "excludes": ["/error"]
    }
  ],
  "routes": [
    {
      "name": "获取用户列表",
      "path": "/users",
      "method": "GET",
      "responseFile": "users.json",
      "headers": {
        "X-Total-Count": "100"
      }
    },
    {
      "name": "获取用户详情",
      "path": "/users/:id",
      "method": "GET",
      "response": {
        "id": "{{params.id}}",
        "name": "用户{{params.id}}",
        "email": "user{{params.id}}@example.com",
        "createdAt": "2024-01-01T00:00:00Z"
      }
    },
    {
      "name": "创建新用户",
      "path": "/users",
      "method": "POST",
      "statusCode": 201,
      "response": {
        "id": "{{body.id}}",
        "name": "{{body.name}}",
        "email": "{{body.email}}",
        "createdAt": "{{body.createdAt}}"
      }
    },
    {
      "name": "获取产品列表",
      "path": "/products",
      "method": "GET",
      "responseFile": "products.json",
      "delay": 500
    },
    {
      "name": "获取产品详情",
      "path": "/products/:id",
      "method": "GET",
      "responseFile": "product-detail.json"
    },
    {
      "name": "搜索接口",
      "path": "/search",
      "method": "GET",
      "response": {
        "query": "{{query.q}}",
        "results": [
          {
            "id": 1,
            "title": "搜索结果 {{query.q}}",
            "description": "这是关于 {{query.q}} 的搜索结果"
          }
        ],
        "total": 1
      }
    },
    {
      "name": "错误响应示例",
      "path": "/error",
      "method": "GET",
      "statusCode": 400,
      "response": {
        "error": "Bad Request",
        "message": "这是一个错误响应示例"
      }
    }
  ]
}